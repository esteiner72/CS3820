# CS3820
semester ai project

1. (Windows) npm install nodemon
   (Mac/Linux) sudo npm install -g --force nodemon

2. npm install express 

3. To start server:
    (Windows) npx nodemon
    (Mac/Linux) nodemon